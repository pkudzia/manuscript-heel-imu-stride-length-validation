# Co-author feedback response, 2026-08-24

## Submission layout quality control, 2026-08-25

- Added visible paragraph spacing and removed paragraph indentation in the main LaTeX review copy, matching the supplementary review layout.
- Moved the stride-length variability paragraph before Table 4 so the table no longer strands a single sentence beneath a full-page display.
- Changed supplementary table placement from `[!h]` to `[!ht]`, removing all float-placement warnings without changing table order.
- Added 6-point paragraph spacing and widow control to the Word review copy.
- Kept every Word figure, table, and algorithm placeholder with its complete caption block.
- Added the same paragraph spacing to the Word supplement, prevented row splits, repeated long-table headers, and kept Tables S3--S7 intact on single pages.
- Rebuilt the main and supplementary PDFs and the main Word file. Visual checks covered all 19 main-PDF pages, all six supplementary-PDF pages, and every Word display page.

## Second-pass review (same day, after the log below)

- Completed Drew's phrasing comment. One "fails in two ways" sentence had survived the first pass. The Introduction now uses Drew's suggested construction ("This works for steady walking, but during slow or irregular walking, ...").
- Corrected three numeric cells against the frozen outputs. Gain 0.3 RMSE is 4.10 cm (was printed 4.11, also in Methods and Results prose), gain 0.6 MAE is 2.49 cm (was 2.48), and Serial 7s GaitRite speed SD is 0.196 (was 0.197). All other checked values (~230 cells) reproduce exactly.
- Split the falls citation sentence. Maki (1997) supports stride-to-stride variability predicting falls but attributes gait slowing to fear of falling, so it no longer backs the slower-gait clause. Verghese (2009) supports both.
- Removed the Commandeur (2024) citation from the GR Pulse synchronisation sentence. That paper covers minimum GaitRite strides for reliable gait metrics and does not describe the synchronisation method. The correct reference for the sync method still needs to be identified, possibly Sandra's submitted paper.
- Renamed Supplementary S7 to "Gravity-residual correction gain sensitivity" to match the manuscript-wide renaming, and matched the abstract's "retroactive gravity-residual correction" wording.
- Light prose edits: two-gap framing announced before the first gap paragraph, tightened one Introduction sentence, harmonised the Table 3 caption to "IMU-minus-GaitRite", and reworded the 87%/97% threshold sentence.
- Rebuilt the PDFs and both Word files and verified rendered pages, cross-references, and the rebuilt Word content.

## Manuscript changes

- Replaced the result-claim title with a criterion-validity title that names the sensor, outcome, walking tasks, older-adult population, and cognitive-status groups.
- Reworked the Introduction to distinguish standard ZUPT processing from the two tested modifications. The text now names contralateral stance validation and retroactive gravity-residual correction directly.
- Rephrased the fixed-threshold limitation to remove the repeated “fails in two ways” construction.
- Removed the unused `UL` acronym from the protocol.
- Replaced the claim that accelerometer filtering would distort double integration. The Methods now explain that zero-velocity and residual corrections address integration drift directly.
- Added the exact adaptive gait-event threshold scales, signal-range floors, cadence scaling, and swing-confirmation rule.
- Explained that stance-midpoint stride times use the same stationary anchors as stride-length integration. Heel-strike timing remains a separate GaitRite comparison.
- Renamed “retroactive orientation-drift correction” to “retroactive gravity-residual correction.” The revised text states that it corrects the residual acceleration left after orientation tracking and gravity removal.
- Stated that the 0.5 correction gain was selected empirically in the validation cohort. The text now flags the lack of independent tuning and reports the full 0.3 to 0.7 sensitivity range.
- Retained Algorithm 1 as the complete processing summary and strengthened its introductory text. We evaluated a separate flowchart but removed it because it duplicated the Methods and algorithm. Both co-authors described the diagram as optional.
- Replaced participant initials with numeric study codes throughout the main manuscript and supplement.
- Corrected the erroneous participant-mean RMSE of 1.2 cm. The frozen data give a mean per-participant RMSE of 3.8 cm, with a 2.1 to 7.2 cm range. The duplicated Discussion sentence was removed.
- Removed the former condition-accuracy figure. Its participant-condition values were valid but easy to confuse with pooled participant MAE. The condition results remain in Table 3 and the Results text.
- Added a short Discussion paragraph that defines the immediate application as supervised straight-pass gait assessment and excludes diagnostic or unsupervised use.

## Feedback not implemented as a new analysis

- We did not add leave-one-participant-out correction-gain tuning. Drew described this as a possible response to future reviewer feedback. The manuscript instead discloses same-cohort tuning and reports the prespecified gain sensitivity analysis.
- We did not change stride-time endpoints to heel strikes. The revision explains why the paired stride-time outcome uses the same stationary endpoints as the integrated displacement. The manuscript already reports heel-strike timing against GaitRite separately.

## Package changes

- Created `peerj_2026-08-24_submission` from the August 10 co-author package.
- Added this response log, a submission checklist, figure captions, and a package README.
- Rebuilt the LaTeX and Word deliverables.
- Added a tested repair for invalid OMML generated by Pandoc. Both Word archives now pass schema validation.
- Updated `wlpeerj.cls` from v1.1 to the official v1.2 structure and replaced the improvised corresponding-author affiliation with `\corrauthor`.
- Added the complete University of Victoria mailing address to the author cover page.
- Renamed the main Methods section to `Materials & Methods`, matching PeerJ's standard research-article structure.
- Removed the old co-author review spacing and abstract-color overrides so the LaTeX PDF uses the official `wlpeerj` submission typography.
- Renamed the artwork files to `Figure1.png`, `Figure2.pdf`, and `Figure3.pdf` for direct journal upload.
- Reformatted the main Word file as US Letter with 2.54 cm margins, 12-point Times New Roman body text, left alignment, double spacing, continuous line numbers, and page numbers.
- Added `PEERJ_TEMPLATE_AUDIT.md` to record the template and current-instruction checks.
- Added the 31-character short title `Heel IMU stride-length validity` to the submission checklist.
